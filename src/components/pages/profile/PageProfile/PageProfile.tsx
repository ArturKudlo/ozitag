import React from 'react';

export const PageProfile = () => {
    return (
        <div>
            <h2>Page Profile</h2>
            <button onClick={() => console.log('log out')}>Log Out</button>
        </div>
    )
}